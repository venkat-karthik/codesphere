# Backend API base URL (NestJS / Express server)
NEXT_PUBLIC_API_URL=http://localhost:3001

# Admin panel secret key — set a strong random value in production
# Used server-side only via /api/admin/verify-key
ADMIN_SECRET_KEY=your-strong-admin-secret-here
