import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { VisualEditsMessenger } from "orchids-visual-edits";
import { Toaster } from "@/components/ui/sonner";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CodeSphere — The Complete Coding Ecosystem",
  description:
    "Learn, build, collaborate, and grow. CodeSphere combines structured learning, vibrant communities, live sessions, global events, and collaborative workspaces in one powerful platform.",
  keywords: ["coding", "learning", "community", "developers", "live sessions", "coding bootcamp"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className="dark">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
        <Toaster />
        <VisualEditsMessenger />
      </body>
    </html>
  );
}
